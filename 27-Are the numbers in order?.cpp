bool isAscOrder(std::vector<int> arr)
{
  if(is_sorted(arr.begin(),arr.end()))
  return true;
  else
  return false;
  }
 /*for(int i=1;i<arr.size();i++){
  if(arr[i]<arr[i-1])
  return false;
  else
  return true;
  */
  
